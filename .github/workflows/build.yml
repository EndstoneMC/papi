name: Build

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to build"
        required: true
        type: string

permissions:
  contents: read

jobs:
  native-linux:
    name: Native (Linux)
    runs-on: ubuntu-22.04
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}

      - name: Install Clang 20
        run: |
          wget -qO /tmp/llvm.sh https://apt.llvm.org/llvm.sh
          chmod +x /tmp/llvm.sh
          sudo /tmp/llvm.sh 20
          sudo apt-get install -y clang-format-20 clang-tidy-20 libc++-20-dev libc++abi-20-dev
          sudo ln -sf /usr/bin/clang-20 /usr/local/bin/clang
          sudo ln -sf /usr/bin/clang++-20 /usr/local/bin/clang++
          clang --version | grep -E "clang version 20\\."

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install Conan
        run: python -m pip install "conan==2.30.0"

      - name: Cache Conan packages
        uses: actions/cache@v6
        with:
          path: .conan2
          key: ${{ runner.os }}-clang20-conan-${{ hashFiles('conanfile.py', '.conan2/profiles/default') }}

      - name: Install dependencies
        run: conan install . --build=missing

      - name: Configure
        run: cmake --preset papi-dev -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build
        run: cmake --build --preset papi-dev

      - name: Run tests
        run: ctest --preset papi-dev --output-on-failure

      - name: Install Python test dependencies
        run: python -m pip install "endstone==0.11.8" pytest

      - name: Stage native module for Python tests
        shell: bash
        run: cp build/RelWithDebInfo/_papi*.so endstone_papi/ 2>/dev/null || true

      - name: Run Python tests
        run: python -m pytest -q

      - name: Check formatting
        run: git ls-files '*.cpp' '*.h' | xargs clang-format-20 --dry-run --Werror

      - name: Run clang-tidy
        run: python tools/run_clang_tidy.py --build-dir build/RelWithDebInfo --clang-tidy clang-tidy-20

      - name: Architecture boundaries
        run: python tests/test_architecture_boundaries.py

      - name: Changelog tooling tests
        run: |
          python tools/test_release_changelog.py
          python tools/test_release_transaction.py
          python tools/test_toolchain_policy.py
          python tools/test_sdist_policy.py

  native-windows:
    name: Native (Windows)
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}

      - name: Set up MSVC environment
        uses: TheMrMilchmann/setup-msvc-dev@368ef7d1ee4d1171b31d4a7f67f4d954f903f5a9 # v4
        with:
          arch: x64

      - name: Select clang-cl 20
        shell: pwsh
        run: |
          $llvmBin = "C:\Program Files\LLVM\bin"
          $llvmBin | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          $version = & "$llvmBin\clang-cl.exe" --version
          $version
          if (-not ($version | Select-String "clang version 20\.")) {
            throw "clang-cl 20 is required"
          }

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install Conan
        run: python -m pip install "conan==2.30.0"

      - name: Cache Conan packages
        uses: actions/cache@v6
        with:
          path: .conan2
          key: ${{ runner.os }}-clang20-conan-${{ hashFiles('conanfile.py', '.conan2/profiles/default') }}

      - name: Install dependencies
        run: conan install . --build=missing

      - name: Configure
        run: cmake --preset papi-dev -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Build
        run: cmake --build --preset papi-dev

      - name: Run tests
        run: ctest --preset papi-dev --output-on-failure

      - name: Install Python test dependencies
        run: python -m pip install "endstone==0.11.8" pytest

      - name: Stage native module for Python tests
        shell: bash
        run: cp build/RelWithDebInfo/_papi*.pyd endstone_papi/ 2>/dev/null || true

      - name: Run Python tests
        run: python -m pytest -q

      - name: Check formatting
        shell: pwsh
        run: |
          $files = git ls-files '*.cpp' '*.h'
          & clang-format --dry-run --Werror $files
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Run clang-tidy
        run: python tools/run_clang_tidy.py --build-dir build/RelWithDebInfo --clang-tidy clang-tidy

      - name: Architecture boundaries
        run: python tests/test_architecture_boundaries.py

  python:
    name: Python
    runs-on: ubuntu-22.04
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install ruff
        run: python -m pip install ruff

      - name: Check formatting
        run: python -m ruff format --check .

      - name: Lint
        run: python -m ruff check .

  wheel:
    name: Wheel (${{ matrix.python-tag }}-${{ matrix.platform }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-22.04, windows-latest ]
        python-tag: [ "cp310", "cp311", "cp312", "cp313", "cp314" ]
        include:
          - os: ubuntu-22.04
            platform: manylinux_x86_64
          - os: windows-latest
            platform: win_amd64
          - python-tag: "cp310"
            python-version: "3.10"
          - python-tag: "cp311"
            python-version: "3.11"
          - python-tag: "cp312"
            python-version: "3.12"
          - python-tag: "cp313"
            python-version: "3.13"
          - python-tag: "cp314"
            python-version: "3.14"

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}
          fetch-depth: 0

      - name: Set up MSVC (Windows)
        if: runner.os == 'Windows'
        uses: TheMrMilchmann/setup-msvc-dev@368ef7d1ee4d1171b31d4a7f67f4d954f903f5a9 # v4
        with:
          arch: x64

      - name: Set up CMake and Ninja
        uses: lukka/get-cmake@fffaaafeea488556c2c12dad60690008bc1caacb # v4.4.2

      - name: Build wheels
        uses: pypa/cibuildwheel@d16cc544083d96996f923fc4b318fee3cb14b34e # v3.4.1
        env:
          CIBW_BUILD: ${{ matrix.python-tag }}-${{ matrix.platform }}
          CIBW_BUILD_VERBOSITY: 1
          CIBW_CONTAINER_ENGINE: "docker; create_args: --volume ${{ github.workspace }}/.conan2:/project/.conan2"

      - name: Reclaim Conan home (Linux)
        if: always() && runner.os == 'Linux'
        run: sudo chown -R "$(id -u):$(id -g)" .conan2

      - name: Verify wheel contents
        shell: bash
        run: |
          python -c "
          import zipfile, sys, pathlib
          whl = list(pathlib.Path('wheelhouse').glob('*.whl'))[0]
          z = zipfile.ZipFile(whl)
          names = z.namelist()
          assert any('_papi' in n for n in names), 'native module missing'
          assert any(n.endswith('.pyi') for n in names), '.pyi missing'
          assert any('include/endstone_papi' in n for n in names), 'headers missing'
          assert any('LICENSE' in n for n in names), 'LICENSE missing'
          provenance = z.read('endstone_papi/_toolchain_provenance.txt').decode()
          assert 'compiler_id=Clang' in provenance, provenance
          assert 'compiler_version=20.' in provenance, provenance
          if sys.platform.startswith('linux'):
              assert 'endstone_papi/libc++.so.1' in names, 'libc++ runtime bridge missing'
              assert 'endstone_papi/libc++abi.so.1' in names, 'libc++abi runtime bridge missing'
              assert not any('endstone_papi.libs/' in n for n in names), 'duplicate C++ runtime bundled'
          # Verify the Endstone runtime dependency is declared.
          meta = [n for n in names if n.endswith('METADATA')][0]
          content = z.read(meta).decode()
          assert any(
              line.lower().startswith('requires-dist: endstone')
              for line in content.splitlines()
          ), 'endstone dependency not declared in wheel METADATA'
          print(f'OK: {len(names)} files in {whl.name}')
          "
          if [ "${{ runner.os }}" = "Linux" ]; then
            docker run --rm \
              -v "$GITHUB_WORKSPACE:/project" \
              -w /project \
              ghcr.io/endstonemc/manylinux_2_31_x86_64 \
              bash -lc 'export PATH=/opt/python/cp312-cp312/bin:$PATH; python -m pip install "auditwheel==6.4.2" "endstone==0.11.8" "patchelf==0.17.2.4" && python tools/verify_linux_wheel.py wheelhouse/*.whl --auditwheel'
          fi

      - name: Set up Python for runtime test
        uses: actions/setup-python@v7
        with:
          python-version: "${{ matrix.python-version }}"

      - name: Runtime smoke test
        shell: bash
        env:
          PYTHONFAULTHANDLER: "1"
        run: |
          python -m pip install endstone==0.11.8
          whl="$(python -c 'import pathlib; print(list(pathlib.Path("wheelhouse").glob("*.whl"))[0])')"
          python -m pip install "$whl"
          python tools/wheel_smoke_test.py

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: papi-${{ matrix.python-tag }}-${{ matrix.platform }}
          path: ./wheelhouse/*.whl

  sdist:
    name: Sdist
    runs-on: ubuntu-22.04
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: ${{ inputs.ref || github.sha }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Install build
        run: python -m pip install build cibuildwheel==3.4.1

      - name: Build sdist
        run: python -m build --sdist

      - name: Verify sdist contains native sources
        shell: bash
        run: |
          python -c "
          import tarfile, pathlib
          sdist = list(pathlib.Path('dist').glob('*.tar.gz'))[0]
          t = tarfile.open(sdist)
          names = t.getnames()
          for required in ['module.cpp', 'gil_safe_proxy.cpp', 'expansion_trampoline.cpp',
                           'bootstrap.cpp', 'placeholder_api_impl.cpp', 'build_linux_runtime.py']:
              assert any(required in n for n in names), f'{required} missing from sdist'
          print(f'OK: sdist contains all native sources')
          "

      - name: Stage only the sdist archive
        shell: bash
        run: |
          rm -rf /tmp/papi-sdist-input /tmp/papi-sdist-build /tmp/papi-sdist-smoke
          mkdir /tmp/papi-sdist-input /tmp/papi-sdist-build /tmp/papi-sdist-smoke
          cp dist/*.tar.gz /tmp/papi-sdist-input/
          test "$(find /tmp/papi-sdist-input -maxdepth 1 -type f | wc -l)" -eq 1

      - name: Extract isolated sdist archive
        shell: bash
        run: |
          tar -xf /tmp/papi-sdist-input/*.tar.gz -C /tmp/papi-sdist-build --strip-components=1

      - name: Build checkout and isolated sdist wheels at Endstone baseline
        shell: bash
        env:
          CIBW_CONTAINER_ENGINE: "docker; create_args: --volume ${{ github.workspace }}/.conan2:/project/.conan2"
        run: |
          CIBW_BUILD=cp312-manylinux_x86_64 python -m cibuildwheel --platform linux \
            --output-dir /tmp/papi-checkout-wheel .
          cd /tmp/papi-sdist-build
          CIBW_BUILD=cp312-manylinux_x86_64 python -m cibuildwheel --platform linux \
            --output-dir "$GITHUB_WORKSPACE/wheelhouse" .

      - name: Reclaim Conan home
        if: always()
        run: sudo chown -R "$(id -u):$(id -g)" .conan2

      - name: Verify checkout and sdist wheel runtime contracts
        shell: bash
        run: |
          sdist_wheel="$(find wheelhouse -maxdepth 1 -name '*.whl' -print -quit)"
          checkout_wheel="$(find /tmp/papi-checkout-wheel -maxdepth 1 -name '*.whl' -print -quit)"
          docker run --rm \
            -v "$GITHUB_WORKSPACE:/project" \
            -v /tmp/papi-checkout-wheel:/checkout-wheel:ro \
            -w /project \
            ghcr.io/endstonemc/manylinux_2_31_x86_64 \
            bash -lc 'export PATH=/opt/python/cp312-cp312/bin:$PATH; python -m pip install "auditwheel==6.4.2" "endstone==0.11.8" "patchelf==0.17.2.4" && python tools/verify_linux_wheel.py wheelhouse/*.whl --compare /checkout-wheel/*.whl --auditwheel'

      - name: Runtime smoke test from isolated sdist-built wheel
        shell: bash
        run: |
          python -m venv /tmp/papi-sdist-smoke-env
          /tmp/papi-sdist-smoke-env/bin/pip install wheelhouse/*.whl
          cd /tmp/papi-sdist-smoke
          /tmp/papi-sdist-smoke-env/bin/python "$GITHUB_WORKSPACE/tools/wheel_smoke_test.py"
          module="$(find /tmp/papi-sdist-smoke-env -path '*site-packages/endstone_papi/_papi*.so' -print -quit)"
          ldd "$module" | tee /tmp/papi-sdist-ldd.txt
          grep -F 'endstone_papi/libc++.so.1' /tmp/papi-sdist-ldd.txt
          grep -F 'endstone_papi/libc++abi.so.1' /tmp/papi-sdist-ldd.txt
          grep -F 'endstone.libs/libunwind-' /tmp/papi-sdist-ldd.txt
          python -c "
          from pathlib import Path
          lines = Path('/tmp/papi-sdist-ldd.txt').read_text().splitlines()
          runtime = [line for line in lines if any(name in line for name in ('libc++.so', 'libc++abi.so', 'libunwind.so'))]
          assert runtime, lines
          assert not any('=> /usr/' in line for line in runtime), runtime
          "
          hashed="$(find /tmp/papi-sdist-smoke-env -path '*site-packages/endstone.libs/libc++-*.so.1.0' -print -quit)"
          test -n "$hashed"
          mv "$hashed" "$hashed.missing"
          if /tmp/papi-sdist-smoke-env/bin/python -c 'import endstone_papi'; then
            echo 'native import unexpectedly succeeded without Endstone libc++' >&2
            exit 1
          fi
          mv "$hashed.missing" "$hashed"

      - uses: actions/upload-artifact@v7
        with:
          name: papi-sdist
          path: ./dist/*.tar.gz
